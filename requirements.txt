# Core
neo4j==5.15.0
streamlit==1.29.0

# NLP
spacy==3.7.2
langdetect==1.0.9

# Data Processing
lxml==5.1.0
python-Levenshtein==0.23.0
datasketch>=1.6.4          # MinHash LSH for scalable fuzzy deduplication
sentence-transformers>=2.7.0  # Phase 3: semantic embeddings
faiss-cpu>=1.8.0              # Phase 3: approximate nearest-neighbor search

# Voice Modules
# ASR (Automatic Speech Recognition)
faster-whisper>=0.10.0

# TTS (Text-to-Speech)
TTS>=0.22.0
soundfile>=0.12.0
gtts>=2.5.0          # lightweight Google TTS for personalized previews
pyttsx3>=2.90        # system TTS for male voice preview (offline)

# Audio Processing
numpy>=1.24.0
torch>=2.0.0
torchaudio>=2.0.0

# Speaker Identification
speechbrain>=0.5.0

# Utilities
python-dotenv==1.0.0
pytest==7.4.3
